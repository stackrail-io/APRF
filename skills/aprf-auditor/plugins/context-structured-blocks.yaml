id: context-structured-blocks
title: Structured instruction vs data context blocks (CTX-R3)
version: "0.2.0"
executor: collectors/context-structured-blocks.ts
evidenceClass: code
alsoEmits: []
detectorIds: [repo-context-structured-blocks]
inputs:
  - "Target repo (context schemas / assemblers)"
  - "Optional suite: aprf-assessment/imports/context-structured-blocks/*.{json}"
collect:
  - "Detect structured instruction/data sections and overwrite tests"
  - "Ingest attestations; write context-structured-blocks-report.json"
emits:
  nodeClass: code
  refPattern: "aprf-assessment/imports/context-structured-blocks/*"
  signals: [context-structured-blocks, ctx-r3, repo-context-structured-blocks]
mapsToChecks: [CTX-R3]
whenUnavailable: >
  Structure signals alone are PARTIAL. CTX-R3 PASS needs
  structuredSectionsEmitted + instructionOverwriteBlocked, measuredAt ≤90 days.
live:
  required: false
  note: Offline structure heuristics + optional imported JSON.
