# Skill package tests

| Test | Command |
| --- | --- |
| Schema + N/A invariants on example fixture | `npx tsx skills/aprf-auditor/tests/validate-fixture.ts` (from APRF root) |

These tests do **not** run a full assessment; they lock the portable output contract agents must emit.
