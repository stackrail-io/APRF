# APRF Check catalog discrepancy audit

Generated by `packages/aprf-engine/scripts/audit-check-discrepancies.py` plus
manual semantic review of high-severity title / passCondition / detector mismatches.

## AUTHZ-M1 (fixed)

| Field | Before | After |
| --- | --- | --- |
| passCondition | 100% automated test coverage of entry points; unauthenticated **or** unauthorized denied | Server-side authz on privileged AI feature/tool/retrieval entry points; **0** successful requests from **authenticated** callers lacking permission/scope |
| evidenceRequired | Authz middleware/policy tests only | Server-side middleware/policy **+** unauthorized-caller suite |
| detectors | Cloud IAM wildcard detectors (wrong domain) | `manual-attest` |
| capability | automated | manual |

Unauthenticated rejection remains **AUTHN-M1**.

## High-severity fixes applied this pass

### Title ↔ passCondition alignment
- **SEC-M3** — gate must be an **abuse/jailbreak/injection** suite, not any “security-suite”
- **SEC2-M3** — added **least-privilege scope** (title said rotatable **and scoped**)
- **AUTHZ-M4** — require **attribute-based** policy predicates (not class inventory alone); restored empty `detectors`
- **PRM-R2** — blocking rules must include **length** + **injection-prone constructs**
- **MEM-M4** — require **cryptographic/signature** verification (not generic integrity)
- **SAF-R2** — require **jailbreak-to-harm** suite distinct from security injection suite

### Detector ↔ check mismatches (wrong automation → honest `manual`)
- **AUTHN-M2** — removed `cicd-oidc-present` (CI OIDC ≠ MCP/S2S machine identity)
- **INF-M1** — removed Docker nonroot/curl-pipe detectors (≠ public exposure scan)
- **TOL-M1** — removed MCP allowlist/fs-scope detectors (≠ per-call gateway authz)
- **AGN-M2** — removed MCP allowlist/fs-scope detectors (≠ agent step/time/spawn limits)
- **REL-M1** — removed CI timeout/concurrency + Docker healthcheck detectors (≠ model/tool client timeouts)
- **SCI-M4** — removed PR-target/self-hosted-runner detectors (≠ unsigned artifact admission)

## Remaining follow-ups (not auto-fixed)

Heuristic keyword scan also flagged many **false positives** (e.g. CHG-M1 “prompt” in title with “prior version” in pass — aligned). Treat those as noise.

Further detector-honest reviews recommended (same pattern: wrong detector would green-light wrong control):
- ORG-M1 / ORG-M2 (`SECURITY.md` / `CODEOWNERS` ≠ AU policy / domain ownership inventory)
- AUTHN-R1 (embedding detectors miss TTL)
- COST-M1 (`cicd-concurrency` ≠ spend throttle)
- INF-M2 (`docker-no-latest-tag` ≠ patching SLA)

Re-run:

```bash
python3 packages/aprf-engine/scripts/audit-check-discrepancies.py
```
