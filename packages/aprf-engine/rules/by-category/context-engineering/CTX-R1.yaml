id: "CTX-R1"
category: "context-engineering"
title: "CTX-R1: Context budgets are monitored per request with alerts on saturation"
description: "Context budgets are monitored per request with alerts on saturation"
whyItMatters: "CTX-R1 (Context Engineering, recommended): Context budgets are monitored per request
  with alerts on saturation Failing this leaves a production gap against: ≥99% of production
  requests in a 24h sample emit context-budget usage; alert fires when usage exceeds the documented
  % of max context (or hard truncate rate ex…"
severity: "high"
weight: 3
gate: "recommended"
passCondition: "≥99% of production requests in a 24h sample emit context-budget usage; alert fires
  when usage exceeds the documented % of max context (or hard truncate rate exceeds threshold)"
evidenceRequired:
  - "Per-request context token/budget metrics dashboard + alert rule export for saturation
    thresholds"
detection:
  capability: "manual"
  detectors:
    - id: "manual-attest"
      params:
        hint: "Per-request context token/budget metrics dashboard + alert rule export for saturation
          thresholds"
manualVerification: "For CTX-R1 (CTX-R1: Context budgets are monitored per request with alerts on
  saturation): inspect current evidence for [Per-request context token/budget metrics dashboard +
  alert rule export for saturation thresholds] and confirm the pass condition holds — ≥99% of
  production requests in a 24h sample emit context-budget usage; alert fires when usage exceeds the
  documented % of max context (or hard truncate rate exceeds threshold)"
falsePositiveGuidance: "CTX-R1 (Context Engineering): re-verify against a current artifact for this
  specific Check (CTX-R1), not a sibling control. Document named exceptions with owner and expiry."
recommendedFixes:
  - "Implement and operationalize: CTX-R1: Context budgets are monitored per request with alerts on
    saturation (CTX-R1)"
  - "Retain evidence artifacts required by this Check, starting with: Per-request context
    token/budget metrics dashboard + alert rule export for saturation thresholds"
  - "Schedule recurring manual verification for CTX-R1 with a named owner and retained report"
  - "Block release (or open a time-boxed waiver with owner and expiry) until CTX-R1 passes"
references:
  - title: "NIST AI RMF — Map and Measure functions"
    url: "https://www.nist.gov/itl/ai-risk-management-framework"
  - title: "OWASP LLM — Sensitive Information Disclosure"
    url: "https://owasp.org/www-project-top-10-for-large-language-model-applications/"
relatedRules:
  - "CTX-M1"
  - "CTX-M2"
  - "CTX-M3"
  - "CTX-R2"
  - "CTX-R3"
tags:
  - "context-engineering"
  - "recommended"
  - "manual"
applicability:
  technologies:
    - "rag-pipelines"
    - "prompt-templates"
  minCriticality: 2
  requiredFromLevel: 4
status: "active"
introducedIn: "0.10.0"
