id: "CTX-R3"
category: "context-engineering"
title: "CTX-R3: Structured context blocks (JSON/XML sections) separate instructions from data"
description: "Structured context blocks (JSON/XML sections) separate instructions from data"
whyItMatters: "CTX-R3 (Context Engineering, recommended): Structured context blocks (JSON/XML
  sections) separate instructions from data Failing this leaves a production gap against: Production
  assembler emits labeled structured sections (e.g. JSON/XML); untrusted data cannot overwrite the
  instruction section in a red-team or unit test"
severity: "high"
weight: 3
gate: "recommended"
passCondition: "Production assembler emits labeled structured sections (e.g. JSON/XML); untrusted
  data cannot overwrite the instruction section in a red-team or unit test"
evidenceRequired:
  - "Context assembly schema/spec separating instruction vs data blocks + sample rendered request"
detection:
  capability: "manual"
  detectors:
    - id: "manual-attest"
      params:
        hint: "Context assembly schema/spec separating instruction vs data blocks + sample rendered request"
manualVerification: "For CTX-R3 (CTX: Structured context blocks (JSON/XML sections) separate
  instructions from data): inspect current evidence for [Context assembly schema/spec separating
  instruction vs data blocks + sample rendered request] and confirm the pass condition holds —
  Production assembler emits labeled structured sections (e.g. JSON/XML); untrusted data cannot
  overwrite the instruction section in a red-team or unit test"
falsePositiveGuidance: "CTX-R3 (Context Engineering): re-verify against a current artifact for this
  specific Check (CTX-R3), not a sibling control. Document named exceptions with owner and expiry."
recommendedFixes:
  - "Implement and operationalize: CTX-R3: Structured context blocks (JSON/XML sections) separate
    instructions from data (CTX-R3)"
  - "Retain evidence artifacts required by this Check, starting with: Context assembly schema/spec
    separating instruction vs data blocks + sample rendered request"
  - "Schedule recurring manual verification for CTX-R3 with a named owner and retained report"
  - "Block release (or open a time-boxed waiver with owner and expiry) until CTX-R3 passes"
references:
  - title: "NIST AI RMF — Map and Measure functions"
    url: "https://www.nist.gov/itl/ai-risk-management-framework"
  - title: "OWASP LLM — Sensitive Information Disclosure"
    url: "https://owasp.org/www-project-top-10-for-large-language-model-applications/"
relatedRules:
  - "CTX-M1"
  - "CTX-M2"
  - "CTX-M3"
  - "CTX-R1"
  - "CTX-R2"
tags:
  - "context-engineering"
  - "recommended"
  - "manual"
applicability:
  technologies:
    - "rag-pipelines"
    - "prompt-templates"
  minCriticality: 2
  requiredFromLevel: 3
status: "active"
introducedIn: "0.10.0"
