id: "AGN-M2"
category: "agent-governance"
title: "Hard limits must exist on steps, wall-clock time, and recursive agent spawning"
description: "Hard limits shall exist on steps, wall-clock time, and recursive agent spawning"
whyItMatters: "AGN-M2 (Agent Governance, mandatory): Hard limits shall exist on steps, wall-clock
  time, and recursive agent spawning Failing this leaves a production gap against: 100% of
  production agents have finite max-steps, wall-clock timeout, and spawn-depth ≤ configured bound;
  tests show limits are enforced (run aborts) when excee…"
severity: "critical"
weight: 4
gate: "mandatory"
passCondition: "100% of production agents have finite max-steps, wall-clock timeout, and spawn-depth
  ≤ configured bound; tests show limits are enforced (run aborts) when exceeded"
evidenceRequired:
  - "Runtime config for max steps, wall-clock timeout, and spawn depth + enforcement tests"
detection:
  capability: "automated"
  detectors:
    - id: "mcp-filesystem-scope"
      params: {}
    - id: "mcp-tool-allowlist"
      params: {}
    - id: "manual-attest"
      params:
        hint: "Runtime config for max steps, wall-clock timeout, and spawn depth + enforcement tests"
manualVerification: "For AGN-M2 (Hard limits must exist on steps, wall-clock time, and recursive
  agent spawning): inspect current evidence for [Runtime config for max steps, wall-clock timeout,
  and spawn depth + enforcement tests] and confirm the pass condition holds — 100% of production
  agents have finite max-steps, wall-clock timeout, and spawn-depth ≤ configured bound; tests show
  limits are enforced (run aborts) when exceeded"
falsePositiveGuidance: "AGN-M2 (Agent Governance): confirm the detector target matches the
  production path for this Check before waiving. Named exceptions need an owner and expiry ≤90
  days."
recommendedFixes:
  - "Implement and operationalize: Hard limits must exist on steps, wall-clock time, and recursive
    agent spawning (AGN-M2)"
  - "Retain evidence artifacts required by this Check, starting with: Runtime config for max steps,
    wall-clock timeout, and spawn depth + enforcement tests"
  - "Wire or verify detectors declared on AGN-M2 so automation matches the pass condition"
  - "Block release (or open a time-boxed waiver with owner and expiry) until AGN-M2 passes"
references:
  - title: "OWASP LLM — Excessive Agency"
    url: "https://owasp.org/www-project-top-10-for-large-language-model-applications/"
  - title: "NIST AI RMF — Govern"
    url: "https://www.nist.gov/itl/ai-risk-management-framework"
relatedRules:
  - "AGN-M1"
  - "AGN-M3"
  - "AGN-M4"
  - "AGN-R1"
  - "AGN-R2"
tags:
  - "agent-governance"
  - "mandatory"
  - "automated"
applicability:
  technologies:
    - "a2a"
    - "mcp"
  minCriticality: 2
  requiredFromLevel: 3
status: "active"
introducedIn: "0.10.0"
