id: "AGN-R2"
category: "agent-governance"
title: "Production systems should have simulation/sandbox runs for new agent behaviors before production"
description: "Simulation/sandbox runs for new agent behaviors before production"
whyItMatters: "Simulation/sandbox runs for new agent behaviors before production Failing this leaves
  a production gap against: Last new agent behavior promoted to production has a linked sandbox run
  ≤30 days before release with pass/fail criteria recorded"
severity: "critical"
weight: 4
gate: "recommended"
passCondition: "Last new agent behavior promoted to production has a linked sandbox run ≤30 days
  before release with pass/fail criteria recorded"
evidenceRequired:
  - "Sandbox/simulation environment config for agents + last pre-prod simulation report for a
    behavior change"
detection:
  capability: "manual"
  detectors:
    - id: "manual-attest"
      params:
        hint: "Sandbox/simulation environment config for agents + last pre-prod simulation report for a
          behavior change"
manualVerification: "For this Check (Simulation/sandbox runs for new agent behaviors before
  production): inspect current evidence for [Sandbox/simulation environment config for agents + last
  pre-prod simulation report for a behavior change] and confirm the pass condition holds — Last new
  agent behavior promoted to production has a linked sandbox run ≤30 days before release with
  pass/fail criteria recorded"
falsePositiveGuidance: "(Agent Governance): re-verify against a current artifact for this specific
  Check , not a sibling control. Document named exceptions with owner and expiry."
recommendedFixes:
  - "Implement and operationalize: this Check: Simulation/sandbox runs for new agent behaviors
    before production"
  - "Retain evidence artifacts required by this Check, starting with: Sandbox/simulation environment
    config for agents + last pre-prod simulation report for a behavior change"
  - "Schedule recurring manual verification for this Check with a named owner and retained report"
  - "Block release (or open a time-boxed waiver with owner and expiry) until this Check passes"
references:
  - title: "OWASP LLM — Excessive Agency"
    url: "https://owasp.org/www-project-top-10-for-large-language-model-applications/"
  - title: "NIST AI RMF — Govern"
    url: "https://www.nist.gov/itl/ai-risk-management-framework"
relatedRules:
  - "AGN-M1"
  - "AGN-M2"
  - "AGN-M3"
  - "AGN-M4"
  - "AGN-R1"
tags:
  - "agent-governance"
  - "recommended"
  - "manual"
applicability:
  technologies:
    - "a2a"
    - "mcp"
  minCriticality: 2
  requiredFromLevel: 4
status: "active"
introducedIn: "0.10.0"
